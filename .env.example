# backend only — never expose to the browser bundle
SPLITCIRCLE_HMAC_KEY=replace-with-32-byte-random-hex
PORT=8787
CIRCLES_RPC_URL=https://rpc.aboutcircles.com/
PATHFINDER_RPC_URL=https://rpc.circlesubi.network/
# storage file for the demo JSON store
SPLITCIRCLE_DB=./data/splitcircle.json

# frontend (VITE_ prefix is exposed; keep only public values here)
VITE_API_BASE=http://localhost:8787
